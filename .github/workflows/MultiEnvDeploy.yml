# ⚠️ WORKFLOW DESABILITADO
# Este workflow foi substituído pelo AdvancedDeploy.yml
# que oferece melhor controle de ambientes e detecção automática.

name: "[DEPRECATED] Multi Environment Deploy"

# Workflow desabilitado - só executa manualmente para mostrar aviso
on:
  workflow_dispatch:
    inputs:
      notice:
        description: 'Este workflow está descontinuado'
        required: false
        default: 'Use AdvancedDeploy.yml'

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: � Aviso de depreciação
        run: |
          echo "⚠️ Este workflow foi descontinuado"
          echo "� Use o workflow 'AdvancedDeploy.yml' que oferece:"
          echo "   - Detecção automática de ambiente"
          echo "   - Suporte a 3 ambientes (dev/staging/prod)"
          echo "   - Configurações organizadas por porta"
          echo "   - Melhor logging e monitoramento"